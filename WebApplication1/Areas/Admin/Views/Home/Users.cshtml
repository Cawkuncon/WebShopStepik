@model List<UserReg>
<div class="row">
    <div class="col-3">
        @await Html.PartialAsync("Index")
    </div>
    <div class="col-9">
        <h2>Список пользователей</h2>
        @foreach (var item in Model)
        {
            <div class="row">
                <div class="col-3"><h3>@item.Name</h3></div>
            </div>

        }
    </div>
</div>
